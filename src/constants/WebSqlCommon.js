const db = openDatabase("problem-interface", "1.0", "desc", 2 * 1024 * 1024);
db.transaction(function (tx) {
    //创建table
    tx
})
function insertProjectProblem() {

}


export default {
    insertProjectProblem
}
